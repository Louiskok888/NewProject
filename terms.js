// concat() 
// Join several arrays into one 

// indexOf() 
// Returns the first position at which a given element appears in an array 

// join() 
// Combine elements of an array into a single string and return the string

// lastIndexOf() 
// Gives the last position at which a given element appears in an array  
                                                                                      
// pop() 
// Removes the last element of an array  

// push() 
// Add a new element at the end

// reverse() 
// Reverse the order of the elements in an array 

// shift() 
// Remove the first element of an array 

// slice() 
// Pulls a copy of a portion of an array into a new array of 4 24 

// sort() 
// Sorts elements alphabetically  

// splice() 
// Adds elements in a specified way and position 

// toString() 
// Converts elements to strings 

// unshift() 
// Adds a new element to the beginning

// valueOf() 
// Returns the primitive value of the specified object

// fill() 
// method in JavaScript is used to change all the elements in an array to a specific static value, 
// from a start index to an end index (optional). It directly modifies the array it is called on.
// array.fill(value, start, end);

// filter()
// creates a new array with all elements that pass a test implemented by the provided function. 
// It does not modify the original array and only returns the elements that satisfy the condition specified in the callback function.
// array.filter(callback(currentValue, index, array), thisArg);

// find()
// used to find the first element in an array that satisfies a provided testing function. 
// It returns the first matching element or undefined if no element satisfies the condition.
// array.find(callback(element, index, array), thisArg);

//forEach()
// to execute a provided function once for each element in an array, in order. 
// It is a non-mutating operation, meaning it does not modify the original array.
// array.forEach(callback(currentValue, index, array), thisArg);

// flat()
// to flatten an array of arrays (or nested arrays) into a single, one-dimensional array.
// array.flat(depth);

// map()
// to create a new array by applying a function to each element of an existing array. The original array is not modified
// array.map(callback(currentValue, index, array), thisArg);

// reduce()
// to accumulate or reduce an array to a single value. It iterates over the array and applies a callback function to each element, passing along the accumulated result.
// array.reduce(callback(accumulator, currentValue, currentIndex, array), initialValue)

// flatMap()
// It first applies a function to each element in an array (like .map() does), and then flattens the result into a new array (like .flat() does).
// array.flatMap(callback(currentValue, index, array), thisArg)

// split()
// to divide a string into an array of substrings based on a specified delimiter or pattern.
// string.split([separator[, limit]])

// reduceRight()
// similar to reduce(), but it processes the elements of an array from right to left 
// (i.e., starting from the last element and working towards the first), rather than from left to right.
// array.reduceRight(callback(accumulator, currentValue, currentIndex, array), initialValue);

// isArray()
// to check if a given value is an array. It returns true if the value is an array, and false otherwise.
// Array.isArray(value)



// Tenary operator

// git add .
// git commit -m "message"
// git pull
// git push
